#!/bin/bash
# Pre-deploy hook — runs on the CI runner before deploying.
#
# Migrations are handled in docker-entrypoint.sh (runs inside the container
# on startup with env vars available), not here. The pre-deploy hook runs
# before Kamal syncs the env file, so secrets aren't available at this point.

exit 0

"""
Chainlit application for the Scout data agent platform.

This package provides the web-based chat interface for interacting with
the Scout data agent. It handles authentication, project selection, and
streaming responses from the LangGraph agent.
"""
